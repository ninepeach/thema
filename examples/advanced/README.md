# KHotel Advanced Example

This example demonstrates a multilingual whole-site shell built with Thema,
native `html/template` composition, and the Go standard library HTTP server.

Run it from this directory so the relative Theme Repository path resolves
correctly:

```sh
cd examples/advanced
go run .
```

Open <http://localhost:8080>.
